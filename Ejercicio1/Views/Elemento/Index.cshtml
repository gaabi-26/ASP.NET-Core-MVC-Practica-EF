@model IEnumerable<Ejercicio1.Models.Elemento>

@{
    ViewData["Title"] = "Index";
}

<h1>Elementos</h1>

<p>
    <a asp-action="Create" class="btn btn-success">Crear Nuevo</a>
</p>

<div class="row mb-3">
    <div class="col-sm-5">
        <input id="searchBox" class="form-control" type="text" placeholder="Buscar por Nombre..." autocomplete="off" />
    </div>
</div>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Descripcion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Descripcion)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-outline-primary">Editar</a> |
                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-outline-info">Detalles</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-outline-danger">Eliminar</a>
                </td>
            </tr>
        }
    </tbody>
</table>
@section Scripts {
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            const box = document.getElementById('searchBox');
            const tbody = document.querySelector('table.table tbody');

            box.addEventListener('input', function () {
                const term = box.value.trim().toLowerCase();

                Array.from(tbody.rows).forEach(row => {
                    const descripcion = (row.cells[0]?.textContent || '')
                        .trim()
                        .toLowerCase();

                    row.style.display = descripcion.includes(term) ? '' : 'none';
                });
            });
        });
    </script>
}